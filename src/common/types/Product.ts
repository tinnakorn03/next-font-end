export interface Product {
    product_id: string;
    product_name: string;
    description: string;
    quantity: number;
    price: number;
    image: string; 
};

export interface Product {
    addItem?: number;
}